%p#notice= notice


%header.page-header
  %hgroup
    %h1
      Title:
      = @pitch.title

  %p#location
    Location:
    = @pitch.location

#video.span8
  %iframe{:width => "100%", :height => "400px", :src => @pitch.video_url, :title => "YouTube video player", :allowscriptaccess => "always", :allowFullScreen => "true", :frameborder => "0"}

#side-bar.span3
  #information
    %button.btn.btn-primary{:name => "build"}
      Build it!
    %button.btn.btn-info{:name => "follow"}
      Tell me when it starts!
  #tags
    - @pitch.skill_list.each do |skill|
      %span.label.label-info= skill


%section.span8#description
  Description:
  = @pitch.description
