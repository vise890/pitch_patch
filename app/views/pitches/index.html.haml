.hero-unit
  %h1
    Your ideas are enormous. Don't carry them alone.
  %p
    Find other people who are just as excited about them as you.

  %a.btn.btn-success.btn-large.offset4
    Pitch in!



%ul#pitches.span10
  - @pitches.each do |pitch|
    %div.pitch.row-fluid.box

      %span.index-video.span3
        %iframe{:width => "100%", :height => "150px", :src => "#{youtube_embed_url pitch.video_url}", :title => "YouTube video player", :allowscriptaccess => "always", :allowFullScreen => "true", :frameborder => "0"}

      %span.index-body.span6
        %h2
          = link_to pitch.title, pitch


      %span.index-details.span2.pull-right
        #tags
          - pitch.skill_list.each do |skill|
            %span.label.label-info= skill
        %p.location
          = pitch.location
        %p.edit
          / %span= link_to pitch.title, pitch
          / %span= link_to 'Edit', edit_pitch_path(pitch)
          / %span= link_to 'Destroy', pitch, method: :delete, data: { confirm: 'Are you sure?' }


